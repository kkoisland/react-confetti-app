# React Confetti サンプルアプリ 設計書

## プロジェクト概要

このプロジェクトは、React Confetti ライブラリの解説本のためのサンプルアプリケーションです。
読者が本を読んで理解した後、実際のサンプルで「ああ、こういうことか」と確認できるように設計されています。

**執筆・実装の進め方**: 実装する → 節を書く → 次の節

## 技術仕様（簡潔版）

**技術スタック**:
- React 19.x + TypeScript 5.9.x + Vite
- Tailwind CSS（スタイリング）
- React Router v6（ルーティング）
- react-confetti（紙吹雪ライブラリ）
- Biome（リンター/フォーマッター）

**ルート構造**:
```
/                    → /basic へ自動リダイレクト
/basic               → 第2節 基本の紙吹雪
/countdown           → 第3節 タイマーによる自動制御
/toast               → 第4節 実務での演出パターン
/seasonal            → 第5節 エフェクトのカスタマイズ
/playground          → 第6節 パラメータの探求
```

**デザイン方針**:
- モダンで美しいUI（シンプルな背景で紙吹雪を際立たせる）
- 上部ナビゲーション + メインエリア（紙吹雪を最大限広く見せる）
- ダーク/ライトモード対応（切り替えボタン付き）
- レスポンシブデザイン
- 白背景（ライトモード）/ ダーク背景（ダークモード）で統一

**カラーテーマ**:
- ベース文字色: `text-gray-800` (全体に適用、真っ黒より読みやすい)
- アクセントカラー: `text-indigo-900` (ホバー時、濃い紺色)
- 背景色: `bg-white` (ライトモード)
- ダークモード: `dark:bg-gray-900` / `dark:text-gray-200` (実装済み、localStorage対応)

**レスポンシブ対応の方針**:
1. 基本レイアウト（上部ナビなど）：最初からレスポンシブ対応
2. 各ページ（/basic, /countdown等）：デスクトップ優先で実装、見た目が揃ってから調整

---

## 第1節 はじめに

### 節の内容
- React Confetti とは
  - Confettiはイタリア語で紙吹雪を意味する
  - お祝いや達成時の演出、季節の演出、ゲーミフィケーション、マイルストーン達成などに最適
  - プログラミング初学者が自分で作る教材としても優れている
- インストール方法
- 基本的な使い方（最小サンプルコード含む）
- トリガーのパターン（コード例なし、概念のみ）
  - トグル型（第2節）
  - 自動停止型（第3節）
  - イベント駆動型（第4節）
- サンプルアプリについて（環境情報、GitHub Pages URL、リポジトリURL）

### 実装済み: section00.md作成完了

サンプルアプリにはダーク/ライトモード切り替え機能も実装していますが、
これはReact Confettiの機能ではなく、アプリ全体のUI改善のためのものです。
本書では詳細な説明は省略しますが、実装されたコードを参考にしてください。

### 実装タスク（付録A参照）
- [ ] Tailwind CSSのセットアップ
- [ ] React Routerのセットアップ
- [ ] 基本レイアウト（上部ナビ + ルーティング）作成
- [ ] ダークモード切り替え実装（おまけ機能）
- [ ] React Confettiのインストール: `pnpm add react-confetti` (型定義は含まれています)
- [ ] 最小サンプルの動作確認

---

## 第2節 基本の紙吹雪 (`/basic`)

### 節の構成案
- 2.1 紙吹雪の表示と非表示
- 2.2 stateによる制御
- 2.3 トリガーの設計パターン
  「この実装では、ボタンのクリックがトリガーとなって紙吹雪の表示/非表示を切り替えています。」

### 機能
- シンプルなボタン1つ
- クリックで紙吹雪のON/OFF切り替え
- state管理の基本

### デザイン
- シンプルな白背景（ダークモード時は暗い背景）で紙吹雪を際立たせる
- **ボタン背景**: `bg-gradient-to-r from-blue-200 to-purple-300`

### 実装タスク
- [ ] `/basic` ページの作成
- [ ] state管理（useState）の実装
- [ ] ボタンUIの作成（Tailwind + ガラスモーフィズム）
- [ ] Confettiコンポーネントの条件付きレンダリング
- [ ] ダークモード版の確認・調整

---

## 第3節 タイマーによる自動制御 (`/countdown`)

### 節の構成案
- 3.1 時間ベースのUI制御
- 3.2 useEffectとクリーンアップ
- 3.3 自動トリガーの実装

### 機能
- カウントダウンタイマー（3秒）
- 「スタート」ボタンでカウントダウン開始
- カウントダウン表示：大きな数字のみ（3 → 2 → 1 → 0）
- 0になったら自動で紙吹雪＋お祝いメッセージを表示
- 紙吹雪は5秒後に自動的に消える
- リセットボタンは常に表示（カウント中でもリセット可能）

**お祝いメッセージ候補**（実装時に選択）：
- 「🎉 おめでとう！」
- 「🎊 カウントダウン完了！」
- "🎉 Congratulations!"

### デザイン
- シンプルな白背景（ダークモード時は暗い背景）で紙吹雪を際立たせる
- **ボタン背景**: `bg-gradient-to-br from-pink-200 via-purple-200 to-indigo-300`

### 実装タスク
- [ ] `/countdown` ページの作成
- [ ] カウントダウンタイマーの実装（useEffect + setInterval）
- [ ] カウントダウン表示UI
- [ ] 紙吹雪の自動表示（カウント0で発火）
- [ ] 紙吹雪の自動消去（5秒後）
- [ ] リセットボタン機能
- [ ] ダークモード版の確認・調整

---

## 第4節 実務での演出パターン (`/toast`)

### 節の構成案
- 4.1 トースト通知とは
- 4.2 トースト通知の自作
- 4.3 イベント駆動のUI制御

### 機能
- シンプルなTodoリスト（固定3つのタスク）
- チェックボックスをクリックでタスクを個別に完了
- **3つ全て完了したとき**にトースト通知（「All Completed!」）＋紙吹雪を同時表示
- トースト通知は自作（ライブラリ不使用）
- 紙吹雪は3秒後に自動的に消える
- トースト表示位置：画面上部中央

### デザイン
- シンプルな白背景（ダークモード時は暗い背景）で紙吹雪を際立たせる
- **ボタン背景**: `bg-gradient-to-br from-white via-orange-100 to-pink-200`
- **トースト通知**: `position: fixed` + Tailwindアニメーション（fade-in/fade-out）

### 実装タスク
- [ ] `/toast` ページの作成
- [ ] Todoリストの実装（固定3つ、チェックボックス）
- [ ] トースト通知コンポーネントの自作（useState + setTimeout）
- [ ] チェック時のトースト表示ロジック
- [ ] チェック時の紙吹雪表示ロジック
- [ ] 紙吹雪の自動消去（3秒後）
- [ ] ダークモード版の確認・調整

---

## 第5節 エフェクトのカスタマイズ (`/seasonal`)

### 節の構成案
- 5.1 パラメータによる見た目の変化
- 5.2 色・形・動きのカスタマイズ
- 5.3 テーマ設計の考え方

### 機能
- タブで季節・テーマ切り替え（桜🌸／雪❄️／紅葉🍁／星✨／クリスマス🎄）
- タブをクリックすると自動的に紙吹雪が降る
- それぞれ色・粒子数・落ち方が異なる
- 背景色の変更は行わない（紙吹雪の色変化を際立たせるため）
- デフォルト表示：桜🌸
- タブデザイン：横並びボタン（5つ）
- **パラメータ表示**：現在のテーマのパラメータ（colors, numberOfPieces, gravity, description）を画面に表示

**各テーマのパラメータ案**：
- **桜🌸**: ピンク系（#FFB7C5, #FFC0CB）、粒子数=150、gravity=0.05（ゆっくり優雅）
- **雪❄️**: 白/水色系（#FFFFFF, #E0F2F7）、粒子数=300、gravity=0.02（ゆっくり細かい）
- **紅葉🍁**: オレンジ/赤系（#FF6347, #FFA500）、粒子数=200、gravity=0.08（やや速め）
- **星✨**: 金/黄系（#FFD700, #FFFF00）、粒子数=100、gravity=0.03（少なめで美しい）
- **クリスマス🎄**: 赤/緑/金（#FF0000, #00FF00, #FFD700）、粒子数=250、gravity=0.06（華やか）

### デザイン
- シンプルな白背景（ダークモード時は暗い背景）で紙吹雪を際立たせる
- 季節ごとのテーマは紙吹雪の色・粒子数・動きで表現

### 実装タスク
- [ ] `/seasonal` ページの作成
- [ ] タブUIの実装（5つの横並びボタン）
- [ ] 季節ごとのConfettiパラメータ設定（colors, numberOfPieces, gravity）
- [ ] タブクリック時の自動紙吹雪表示
- [ ] デフォルト表示（桜🌸）の設定
- [ ] パラメータ表示エリアの実装（現在のテーマのパラメータを表示）
- [ ] ダークモード版の確認・調整

---

## 第6節 パラメータの探求 (`/playground`)

### 節の構成案
- 6.1 パラメータの解説
- 6.2 パフォーマンスとのバランス
- 6.3 インタラクティブな調整UI
- 6.4 プログラム制御パラメーター（run, recycle）の実務での使い方
  - `run`: タスク完了時、タブアクティブ時など条件付きでアニメーション制御
  - `recycle`: 一度きりの演出（false）vs 継続的な演出（true）

### 機能
- インタラクティブなプレイグラウンド
- リアルタイムで調整結果が反映される

**調整可能なパラメータ**：
- **スライダー**：
  - numberOfPieces（粒子数：50-500、刻み幅=10）
  - gravity（重力：0-0.3、刻み幅=0.01）
  - wind（風：-0.1-0.1、刻み幅=0.01）
  - initialVelocityX（横初速：-10-10、刻み幅=1）
  - initialVelocityY（縦初速：-20-0、刻み幅=1）
  - friction（摩擦係数：0.9-1.0、刻み幅=0.01）
  - opacity（透明度：0-1、刻み幅=0.1）
- **カラー選択**：
  - ラジオボタン：「Use default colors (17 colors)」または「Custom colors (up to 5)」
  - デフォルト：React Confettiのデフォルト17色を使用
  - カスタム：カラーピッカー5つで自由に色を選択（1-5色、空欄OK）
  - Reset Colorsボタン：デフォルトに戻す

**その他の機能**：
- **Stop/Start Confettiボタン**：紙吹雪の表示/非表示を切り替え
- **プリセットボタン**：第5節の5つのテーマ（桜、雪、紅葉、星、クリスマス）をワンクリック適用（自動的に紙吹雪が再起動）
- **Reset Colorsボタン**：色の設定をデフォルト（17色）に戻す（自動的に紙吹雪が再起動）
- **Reset Parametersボタン**：スライダーのパラメーター（色以外）をデフォルト値に戻す（自動的に紙吹雪が再起動）
- **Reset Allボタン**：全パラメーター（色含む）をデフォルト値に戻す（自動的に紙吹雪が再起動）
- **スライダー調整時の自動再起動**：スライダーを離した瞬間（onMouseUp/onTouchEnd）に紙吹雪が再起動し、正しい挙動で表示される
- **パラメータ値の表示**：各スライダーの隣に現在値とデフォルト値を表示
- **コードスニペット表示**：画面右下に配置、リアルタイム更新、Show All / Changes Only切り替え機能付き
- **Show All / Changes Onlyトグル**：全パラメータ表示と変更分のみ表示を切り替え（学習用と実用を両立）
- **Copy Codeボタン**：コピー成功時に「Copied!」と表示（2秒後に戻る）、ボタン幅固定でレイアウト安定
- **初期状態**：ページを開いたときからデフォルト設定で紙吹雪が降っている

**コードスニペット表示例**：

デフォルトカラー使用時：
```jsx
<Confetti
  numberOfPieces={200}
  gravity={0.1}
  wind={0}
/>
```

カスタムカラー使用時：
```jsx
<Confetti
  numberOfPieces={200}
  gravity={0.1}
  wind={0}
  colors={['#FF0000', '#00FF00', '#0000FF']}
/>
```

※ デフォルト値（recycle=true、colors=undefined など）は省略

### デザイン
- シンプルな白背景（ダークモード時は暗い背景）で紙吹雪を際立たせる
- **カラーテーマ**: 青→紫グラデーション（BasicPageと統一）
  - メインアクションボタン: `from-blue-200 to-purple-300`（ホバー時: `from-blue-400 to-purple-500`）
  - スライダー: 青→紫グラデーションバー、白つまみ（インディゴ枠）
  - ラジオボタン: インディゴ
- **レイアウト**：
  - **上部**：調整UI（6行構成）
    - Row 1：スライダー3つ（numberOfPieces, gravity, wind）横並び（onMouseUp/onTouchEndで自動再起動）
    - Row 2：スライダー2つ（initialVelocityX, initialVelocityY）横並び（onMouseUp/onTouchEndで自動再起動）
    - Row 3：スライダー2つ（friction, opacity）横並び（onMouseUp/onTouchEndで自動再起動）
    - Row 4：カラー選択（ラジオボタン2つ、カスタム選択時はカラーピッカー5つ表示、折り返し対応）
    - Row 5：プリセットボタン5つ（桜、雪、紅葉、星、クリスマス）横並び、控えめなグレー背景
    - Row 6：コントロールボタン4つ（Start/Stop Confetti, Reset Colors, Reset Parameters, Reset All）横並び、折り返し対応
  - **右下**：コードスニペット表示（半透明の白背景、紙吹雪が上も降る、リアルタイム更新、Show All/Changes Onlyトグル、Copy Codeボタン付き）
  - **全体**：紙吹雪が画面全体に降る（showConfettiがtrueの場合のみ、コードスニペットの上も降る）

### 実装タスク
- [x] `/playground` ページの作成
- [x] スライダーUIの実装（numberOfPieces, gravity, wind, initialVelocityX, initialVelocityY, friction, opacity）
- [x] スライダーの隣に現在値とデフォルト値を表示
- [x] カラーピッカーの実装（5色、デフォルト17色との切り替え）
- [x] プリセットボタンの実装（第5節の5テーマ）
- [x] Stop/Start Confettiボタンの実装
- [x] リセットボタンの実装（Reset Colors, Reset Parameters, Reset All）
- [x] スライダー調整時の自動再起動（onMouseUp/onTouchEnd）
- [x] コードスニペット表示エリアの実装（右下固定配置、リアルタイム更新）
- [x] コピーボタンの実装（クリップボードにコピー、成功時に「Copied!」表示）
- [x] 初期状態の紙吹雪表示（デフォルト設定で自動的に降る）
- [x] Show All / Changes Onlyトグルの実装
- [x] スライダーのカスタムスタイリング（青→紫グラデーション）
- [x] モバイル対応（カラーピッカー・ボタンの折り返し）
- [x] カラーテーマの統一（BasicPageと同じグラデーション）
- [ ] ダークモード版の確認・調整

---

## 第7節 高度なパラメータとパフォーマンス

### 節の内容
- Playgroundに含まれていないパラメータ（confettiSource, drawShape, recycle, onConfettiComplete）
- パラメータ一覧リファレンス
- パフォーマンス最適化（スマートフォンでのパフォーマンス対策含む）

### 実装済み: section07.md作成完了

**含まれるパラメータ:**
- `confettiSource`: 発生源の指定
- `drawShape`: カスタム形状
- `recycle`: 自動再生成の制御
- `run`: アニメーションの開始・停止（重要な違いの説明付き）
- `onConfettiComplete`: アニメーション完了時のコールバック

**削除したパラメータ:**
- `tweenDuration`: 実際には効かないため削除

**削除したセクション:**
- トラブルシューティング: 基本を理解した読者には不要な初歩的な内容のため削除

**パフォーマンス最適化:**
- numberOfPiecesをデバイスに応じて調整（スマートフォン対策を統合）
- 不要なときはアンマウント
- recycleをfalseにする
- drawShapeはシンプルに

**runパラメータの重要な説明:**
- `run={false}`はアニメーション停止のみで紙吹雪は残る
- 紙吹雪を完全に消すには条件付きレンダリング（`{showConfetti && <Confetti />}`）を使う
- Basic Pageで条件付きレンダリングを採用した理由が明確になる
- 実装例を2つ追加（Pause/Resumeパターン、一度だけの演出パターン）

**コード例の修正:**
- すべてのサンプルから`width={width}`, `height={height}`を削除（基本的な使い方では不要）

### 実践的な統合パターン（削除済み、将来的に追加を検討）

以下のパターン例は7節から削除しましたが、他の人のフィードバックを見て必要であれば7節または8節に追加できるよう保持しています。

**パターン1: ボタンクリックで一度だけ発生**
- `recycle={false}`と`onConfettiComplete`を使った実装例

**パターン2: 特定の要素からConfettiを発生させる**
- `confettiSource`とuseRefを使った位置指定の実装例

**パターン3: カスタム形状のConfetti**
- `drawShape`を使った星型Confettiの実装例

---

## 第8節 さらなる可能性

### 節の内容
1. 他のUIパターンへの応用
   - コンポーネント種類ごとの活用例（モーダル、フォーム、ローディング）
   - シチュエーション例
   - 実装のヒント（適切な使用場面、ユーザー体験への配慮）
2. Canvas Confettiについて
   - React ConfettiとCanvas Confettiは別のライブラリ
   - Canvas Confettiでできること（花火演出、複数箇所からの発射、時間差発射）
   - 基本的な使い方と例

### 実装済み: section08.md作成完了

**修正内容:**
- `tweenDuration`を`setTimeout`に修正（ユーザー体験への配慮の部分）

---

## 旧：第7節 終わりに（現在は第8節に統合）

### 節の内容
- 他のUIパターンへの応用
- より高度な演出が必要な場合（Canvas Confetti）

### 他のUIパターンへの応用

**コンポーネント種類ごとの活用例**：
- **モーダル（ダイアログ）**：会員登録完了モーダル、レベルアップモーダルなど
- **フォーム送信成功時**：送信ボタンを押して成功レスポンスが返ってきたタイミング
- **ローディング完了時**：時間のかかる処理（データインポート、ファイルアップロードなど）が完了したタイミング

**シチュエーション例**：
ゲームの正解時やレベルアップ時、購入完了時、会員登録完了時、目標達成時、タスク完了時など、ユーザーにとって嬉しい瞬間に紙吹雪を使うことで、体験をより印象的にできます。

**書く内容**：
- 各パターンでの実装のヒント（どのタイミングでstateを切り替えるか）
- 適切な使用場面と避けるべき場面
- ユーザー体験を損なわないための配慮（頻度、タイミング）

### Canvas Confettiについて
React Confettiはシンプルで使いやすいですが、より複雑な演出が必要な場合は
**Canvas Confetti**（https://github.com/catdad/canvas-confetti）も検討してください。

**Canvas Confettiでできること**：
- 花火のような爆発演出
- 複数の発射位置を指定
- より細かい制御（角度、速度、形状など）
- 時間差での複数発射

ただし、設定が複雑になるため、まずはReact Confettiで十分です。
必要に応じて、段階的にCanvas Confettiへ移行することもできます。

---

## 付録A：環境構築手順

**注意**: 実装時の参考として詳細な手順を記載していますが、本に掲載する際は大幅に削除して「補足セットアップ手順」にする可能性があります。理由：
- 初心者には詳しく書いても分からない
- 経験者には公式ドキュメントの方が有用
- 環境の変化ですぐ古くなる
- 誰にも参考にならない可能性が高い

### セットアップ手順（実装時の参考用）

#### 0. スタイリング方針
- **index.css**: すべての必要なCSSをここに集約
- **インラインスタイル**: 必要に応じて使用
- **方針**: CSSファイルを分散させず、index.cssに統一

#### 1. Tailwind CSSのインストールと設定

**インストール**：
```bash
pnpm add -D tailwindcss @tailwindcss/vite
```

**src/index.cssを編集**：
```css
@import "tailwindcss";
```
既存のCSSは削除して、この1行のみにする。

**vite.config.tsを編集**：
```ts
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

**クリーンアップ**：
- `src/App.css` を削除（不要）
- `src/App.tsx` をシンプルなHello Worldに変更

#### 2. React Routerのインストールと設定
```bash
pnpm add react-router-dom
```

#### 3. react-confettiのインストール
```bash
pnpm add react-confetti
```
※型定義は既にパッケージに含まれているため、`@types/react-confetti`は不要です。

#### 4. 基本レイアウト（上部ナビ + ルーティング）

**重要**: この段階でレスポンシブ対応を含めて実装します。各ページ（/basic等）はデスクトップ優先で実装するため、レイアウトだけはここでしっかり作ります。

**実装のポイント**：
- 上部ナビゲーションバー（/basic, /countdown, /toast, /seasonal, /playground）
- メインエリア（各ページのコンテンツ、画面全体を使える）
- レスポンシブデザイン（モバイルではハンバーガーメニュー、デスクトップでは横並びメニュー）
- 紙吹雪が画面全体に広がるよう、メインエリアは最大限の広さを確保

#### 5. ダークモード切り替え

サンプルアプリをより見やすくするため、ダーク/ライトモード切り替え機能を実装します。
これはReact Confettiの機能ではなく、アプリ全体のUI改善のためのおまけ機能です。

**実装のポイント**：
- Tailwindの `dark:` クラスを使用
- 上部ナビの右上に切り替えボタンを配置
- `localStorage` で設定を保存（オプション）
- 各ページのグラデーション背景にダークモード版を用意

本書では詳細な説明は省略しますが、実装されたサンプルコードを参考にしてください。

---

### 本に掲載する場合の簡略版（案）

```markdown
## 付録A：補足セットアップ手順

このサンプルアプリは以下で構成されています：
- Tailwind CSS（スタイリング）
- React Router v6（ルーティング）
- react-confetti（紙吹雪）

各ライブラリの詳細なセットアップ方法は公式ドキュメントを参照してください。
または、リポジトリをクローンしてすぐに動かすこともできます。

[リポジトリURL]
```

---

## 開発ガイドライン

- **コードの可読性**: 初心者が読んで理解できるコード
- **コメント**: 重要な部分には簡潔な英語コメント（グローバルな視点を意識）
- **再利用性**: 共通コンポーネントの活用
- **型安全性**: TypeScriptの恩恵を最大限に
- **美しさ**: Tailwindで洗練されたデザイン

## 参考リンク

- [React Confetti GitHub](https://github.com/alampros/react-confetti/)
- [React Router Documentation](https://reactrouter.com/)
- [Tailwind CSS Documentation](https://tailwindcss.com/)

---

作成日: 2025-11-16
最終更新: 2025-11-23
